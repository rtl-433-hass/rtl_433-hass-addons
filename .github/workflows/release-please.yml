# release-please runs on every push to main: it maintains the release PR and,
# when that PR merges, tags the release + creates the GitHub Release. The tag
# (v*) is what triggers the stable addon build in release.yml.
#
# It authenticates with a short-lived token from an org-owned GitHub App rather
# than the default GITHUB_TOKEN, because:
#   * a PR created by GITHUB_TOKEN does not trigger other workflows (so the
#     release PR would get no CI), whereas an App-created PR does; and
#   * the App token is repo-scoped and not tied to a personal PAT.
#
# Requires two secrets (App ID + PEM private key) from a GitHub App installed on
# this repo with only Contents: read/write and Pull requests: read/write:
#   RELEASE_PLEASE_APP_ID
#   RELEASE_PLEASE_APP_PRIVATE_KEY
# Until those secrets exist, this workflow fails at the token step by design.
#
# This workflow runs only on push to main, so the App secrets are never exposed
# to fork pull-request runs.
name: Release Please

on:
  push:
    branches: [main]

# No GITHUB_TOKEN permissions needed: the App token below carries its own
# Contents/Pull-requests write scopes, and the token step authenticates via the
# App key, not GITHUB_TOKEN.
permissions: {}

jobs:
  release-please:
    runs-on: ubuntu-24.04
    steps:
      - name: Mint a GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.RELEASE_PLEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_PLEASE_APP_PRIVATE_KEY }}

      - uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7 # v5.0.0
        id: release
        with:
          token: ${{ steps.app-token.outputs.token }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      # release-please writes version headings as "## [x.y.z](compare) (date)".
      # Home Assistant's Supervisor, however, only trims the add-on update
      # dialog to the version you're updating *to* when each heading is a bare
      # "## x.y.z" -- it slices CHANGELOG.md with the regex `^#* <version>\n`
      # (homeassistant/components/hassio/update.py: async_release_notes). With
      # the bracketed form the regex never matches and the dialog shows the
      # ENTIRE changelog on every update.
      #
      # Rewrite the headings on the release PR branch so the fix ships as part
      # of the release commit. The date and compare link are preserved on the
      # line below each heading. Rather than adding a second commit, the rewrite
      # is amended into release-please's own commit and force-pushed, so the PR
      # stays a single commit. The sed only matches release-please's bracketed
      # form, so it is idempotent: re-running on an already-normalized file (the
      # usual case, since release-please regenerates the branch on every push)
      # changes nothing and the step exits without amending or pushing.
      #
      # The clone is intentionally NOT shallow. release-please builds the release
      # commit via the GitHub API, so its parent (main's HEAD) is a separate
      # object. With `--depth 1` that parent is not fetched, and `git commit
      # --amend` then rewrites the release commit into a *root* commit -- the
      # branch loses its ancestry to main, the PR shows the entire repo as
      # additions (no merge base), and release-please closes and recreates the
      # PR on every run. A full clone keeps the parent object present so the
      # amend preserves the link back to main.
      #
      # Pushes use the App token in the clone URL, not GITHUB_TOKEN, so the
      # workflow's `permissions: {}` posture is unchanged.
      #
      # This only runs when release-please opened/updated a release PR. On a
      # release-creation run (the PR was merged, a tag was cut) there is no PR
      # and nothing to normalize -- the CHANGELOG was already normalized in the
      # PR that just merged. The `if:` skips the step then, but GitHub still
      # evaluates a step's `env:` even when its `if:` is false, so the PR JSON
      # is passed through verbatim and parsed with jq in the script. Calling
      # `fromJSON()` on the empty `outputs.pr` string in an `env:` expression
      # raises a template error and fails the whole release run.
      - name: Normalize CHANGELOG headings for Home Assistant
        if: ${{ steps.release.outputs.pr }}
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          PR_JSON: ${{ steps.release.outputs.pr }}
        run: |
          set -euo pipefail
          PR_BRANCH="$(printf '%s' "$PR_JSON" | jq -r '.headBranchName')"
          git clone --branch "$PR_BRANCH" \
            "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" rp-branch
          cd rp-branch
          sed -i -E \
            's@^(#{2,}) \[([^]]+)\]\(([^)]+)\) \(([0-9]{4}-[0-9]{2}-[0-9]{2})\)$@\1 \2\n\n_\4_ · [Compare](\3)@' \
            rtl_433/CHANGELOG.md
          if git diff --quiet; then
            echo "CHANGELOG headings already Home Assistant-compatible; nothing to do."
            exit 0
          fi
          # Amend into release-please's commit (preserving its message and
          # author) so the PR keeps a single commit, then force-push the branch.
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit --amend --no-edit -a
          git push --force-with-lease origin "HEAD:${PR_BRANCH}"
